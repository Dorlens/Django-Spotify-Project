function skipSong() {
    // Hide the current iframe (the player)
    document.getElementById("spotify-player").style.display = "none";

    location.reload();  // Refresh the page to load a new song
}

function ThumbUpSong() {
    // Hide the current iframe (the player)
    document.getElementById("spotify-player").style.display = "none";
    

    location.reload();  // Refresh the page to load a new song
}